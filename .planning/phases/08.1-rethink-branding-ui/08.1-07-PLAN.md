---
phase: 08.1-rethink-branding-ui
plan: 07
type: execute
wave: 3
depends_on: ["08.1-01", "08.1-02"]
files_modified:
  - sidekiq-webapp/src/components/team/team-avatar.tsx
  - sidekiq-webapp/src/components/team/team-empty-state.tsx
  - sidekiq-webapp/src/components/team/team-create-dialog.tsx
  - sidekiq-webapp/src/components/team/team-form.tsx
  - sidekiq-webapp/src/components/team/team-settings-section.tsx
  - sidekiq-webapp/src/components/team/team-member-list.tsx
  - sidekiq-webapp/src/components/team/team-member-row.tsx
  - sidekiq-webapp/src/components/team/team-invites-list.tsx
  - sidekiq-webapp/src/components/team/invite-member-dialog.tsx
  - sidekiq-webapp/src/components/team/invite-accept-card.tsx
  - sidekiq-webapp/src/components/team/remove-member-dialog.tsx
  - sidekiq-webapp/src/components/team/delete-team-dialog.tsx
autonomous: true

must_haves:
  truths:
    - "Team components use semantic tokens throughout"
    - "Team avatar keeps rounded-lg shape (distinct from user/sidekiq rounded-full)"
    - "Team empty state is clean with muted icon, no glass"
    - "Dialogs styled as elevated cards (inherited from UI primitive update)"
    - "Member rows have clean hover states"
    - "No hardcoded zinc colors in any team component"
  artifacts:
    - path: "sidekiq-webapp/src/components/team/team-avatar.tsx"
      provides: "Team avatar with rounded-lg shape"
      contains: "rounded-lg"
    - path: "sidekiq-webapp/src/components/team/team-empty-state.tsx"
      provides: "Clean empty state without glass"
    - path: "sidekiq-webapp/src/components/team/team-settings-section.tsx"
      provides: "Settings section with semantic tokens"
  key_links:
    - from: "sidekiq-webapp/src/components/team/team-settings-section.tsx"
      to: "sidekiq-webapp/src/styles/globals.css"
      via: "Semantic tokens"
      pattern: "bg-card"
---

<objective>
Restyle all team components to match the Linear-inspired aesthetic with semantic tokens, clean empty states, and consistent styling.

Purpose: Team components were the most recently built (Phase 8) and may already be cleaner than earlier components, but still need verification and token consistency.
Output: 12 updated team component files.
</objective>

<execution_context>
@/Users/carlocasorzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carlocasorzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-rethink-branding-ui/08.1-RESEARCH.md
@.planning/phases/08.1-rethink-branding-ui/08.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle team display and management components</name>
  <files>
    sidekiq-webapp/src/components/team/team-avatar.tsx
    sidekiq-webapp/src/components/team/team-empty-state.tsx
    sidekiq-webapp/src/components/team/team-settings-section.tsx
    sidekiq-webapp/src/components/team/team-member-list.tsx
    sidekiq-webapp/src/components/team/team-member-row.tsx
    sidekiq-webapp/src/components/team/team-invites-list.tsx
  </files>
  <action>
  Read each file and replace hardcoded colors with semantic tokens:

  **team-avatar.tsx:**
  - Keep `rounded-lg` shape (distinct from user/sidekiq rounded-full per research)
  - Replace any hardcoded zinc colors with semantic tokens
  - Avatar fallback background: keep existing color system or use `bg-muted`

  **team-empty-state.tsx:**
  - Remove `glass-subtle` if present
  - Clean centered layout: icon in `bg-muted rounded-full p-4`, heading in `text-foreground font-semibold`, description in `text-muted-foreground`, primary CTA
  - Replace hardcoded zinc colors

  **team-settings-section.tsx:**
  - Section containers: `bg-card border border-border rounded-lg` for elevated sections
  - Section titles: `text-lg font-semibold text-foreground`
  - Section descriptions: `text-sm text-muted-foreground`
  - Danger zone: `border-destructive/50` for destructive sections
  - Replace hardcoded zinc colors

  **team-member-list.tsx:**
  - Clean list styling with semantic tokens
  - Replace hardcoded zinc colors

  **team-member-row.tsx:**
  - Row: `hover:bg-accent/50 rounded-md px-3 py-2 transition-colors`
  - Member name: `text-sm font-medium text-foreground`
  - Role badge: use badge component with appropriate variant
  - Owner crown/shield icons: `text-primary` for owner, `text-muted-foreground` for admin
  - Replace hardcoded zinc colors

  **team-invites-list.tsx:**
  - Invite rows: similar to member rows with semantic tokens
  - Pending status: `text-muted-foreground text-xs`
  - Expiry info: `text-muted-foreground`
  - Replace hardcoded zinc colors
  </action>
  <verify>
  Run: `grep -r "zinc-\|glass\|backdrop-blur" sidekiq-webapp/src/components/team/team-avatar.tsx sidekiq-webapp/src/components/team/team-empty-state.tsx sidekiq-webapp/src/components/team/team-settings-section.tsx sidekiq-webapp/src/components/team/team-member-list.tsx sidekiq-webapp/src/components/team/team-member-row.tsx sidekiq-webapp/src/components/team/team-invites-list.tsx` — should return no matches.
  </verify>
  <done>
  Team avatar keeps rounded-lg. Empty state is clean. Settings section uses card containers. Member rows have hover states. All use semantic tokens with zero hardcoded colors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle team dialogs and forms</name>
  <files>
    sidekiq-webapp/src/components/team/team-create-dialog.tsx
    sidekiq-webapp/src/components/team/team-form.tsx
    sidekiq-webapp/src/components/team/invite-member-dialog.tsx
    sidekiq-webapp/src/components/team/invite-accept-card.tsx
    sidekiq-webapp/src/components/team/remove-member-dialog.tsx
    sidekiq-webapp/src/components/team/delete-team-dialog.tsx
  </files>
  <action>
  Read each file and replace hardcoded colors:

  **team-create-dialog.tsx:**
  - Dialog content uses UI primitive styling (already updated in Plan 02)
  - Replace any hardcoded zinc colors in the dialog body content
  - Form elements: semantic tokens

  **team-form.tsx:**
  - Form labels: `text-sm font-medium text-foreground`
  - Form inputs: use the semantic input styling (already updated in Plan 02)
  - Form descriptions: `text-xs text-muted-foreground`
  - Replace hardcoded zinc colors

  **invite-member-dialog.tsx:**
  - Tabbed dialog: tabs use semantic tokens (already updated in Plan 02)
  - Email input: semantic tokens
  - Copy link area: `bg-muted rounded-md px-3 py-2` with monospace text for link
  - Replace hardcoded zinc colors

  **invite-accept-card.tsx:**
  - Five states (not found, expired, unauthenticated, wrong email, valid): all use semantic tokens
  - Card: `bg-card border border-border rounded-lg`
  - Success state: use `text-primary` for success messaging
  - Error states: use `text-destructive` for error messaging
  - Replace hardcoded zinc colors

  **remove-member-dialog.tsx:**
  - Warning text: `text-muted-foreground`
  - Destructive button: `bg-destructive text-destructive-foreground`
  - Replace hardcoded zinc colors

  **delete-team-dialog.tsx:**
  - Same treatment as remove-member: destructive action styling
  - Type-to-confirm input: semantic tokens
  - Replace hardcoded zinc colors
  </action>
  <verify>
  Run: `grep -r "zinc-\|glass\|backdrop-blur" sidekiq-webapp/src/components/team/team-create-dialog.tsx sidekiq-webapp/src/components/team/team-form.tsx sidekiq-webapp/src/components/team/invite-member-dialog.tsx sidekiq-webapp/src/components/team/invite-accept-card.tsx sidekiq-webapp/src/components/team/remove-member-dialog.tsx sidekiq-webapp/src/components/team/delete-team-dialog.tsx` — should return no matches.
  Run: `cd sidekiq-webapp && pnpm tsc --noEmit 2>&1 | head -20` — no TypeScript errors.
  </verify>
  <done>
  All team dialog and form files use semantic tokens. Invite accept card handles all 5 states cleanly. Destructive actions use red styling. Forms use semantic input/label tokens. Zero hardcoded colors.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "zinc-\|glass\|backdrop-blur" sidekiq-webapp/src/components/team/` returns no matches
2. `grep "rounded-lg" sidekiq-webapp/src/components/team/team-avatar.tsx` confirms team avatar shape
3. `cd sidekiq-webapp && pnpm tsc --noEmit` passes
</verification>

<success_criteria>
- All 12 team component files use semantic tokens only
- Team avatar keeps rounded-lg shape
- Empty state is clean with no glass
- Settings section uses card containers with borders
- Dialogs inherit elevated card styling from UI primitives
- Member/invite rows have clean hover states
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rethink-branding-ui/08.1-07-SUMMARY.md`
</output>
