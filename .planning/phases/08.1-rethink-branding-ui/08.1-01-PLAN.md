---
phase: 08.1-rethink-branding-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sidekiq-webapp/src/styles/globals.css
  - sidekiq-webapp/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "CSS variables define a blue-accent oklch palette for both light and dark modes"
    - "Inter is the primary sans-serif font loaded via next/font/google"
    - "Geist Mono is available as the monospace font"
    - "Glassmorphism utility classes (.glass, .glass-subtle, .glass-input) are removed from globals.css"
    - "Border radius base is 0.375rem (6px) for sharp technical feel"
    - "--font-sans references --font-inter instead of --font-geist-sans"
  artifacts:
    - path: "sidekiq-webapp/src/styles/globals.css"
      provides: "Complete color token system with blue accent"
      contains: "oklch(0.55 0.2 260)"
    - path: "sidekiq-webapp/src/app/layout.tsx"
      provides: "Inter + Geist Mono font loading"
      contains: "Inter"
  key_links:
    - from: "sidekiq-webapp/src/app/layout.tsx"
      to: "sidekiq-webapp/src/styles/globals.css"
      via: "CSS variable --font-inter set on html element, consumed by @theme --font-sans"
      pattern: "--font-inter"
---

<objective>
Establish the design system foundation: new blue-accent color palette, Inter font, reduced border radius, and glassmorphism removal.

Purpose: All subsequent component updates depend on these token values being correct. This is the single source of truth for the entire visual redesign.
Output: Updated globals.css with new color tokens and layout.tsx with Inter font loading.
</objective>

<execution_context>
@/Users/carlocasorzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carlocasorzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-rethink-branding-ui/08.1-RESEARCH.md
@sidekiq-webapp/src/styles/globals.css
@sidekiq-webapp/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redefine CSS tokens and remove glassmorphism</name>
  <files>sidekiq-webapp/src/styles/globals.css</files>
  <action>
  Replace the entire color token system in globals.css with the new blue-accent oklch palette from the research document. Specific changes:

  1. **@theme block**: Change `--font-sans` from `var(--font-geist-sans)` to `var(--font-inter)`. Add `--font-mono: var(--font-geist-mono), ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;`

  2. **:root (light mode)**: Replace all color values:
     - `--radius: 0.375rem` (was 0.625rem)
     - `--background: oklch(0.985 0.002 260)` (near-white with slight blue)
     - `--foreground: oklch(0.15 0.01 260)` (very dark blue-gray)
     - `--card: oklch(1 0 0)` (pure white)
     - `--card-foreground: oklch(0.15 0.01 260)`
     - `--popover: oklch(1 0 0)`
     - `--popover-foreground: oklch(0.15 0.01 260)`
     - `--primary: oklch(0.55 0.2 260)` (blue accent)
     - `--primary-foreground: oklch(0.985 0 0)` (white on blue)
     - `--secondary: oklch(0.96 0.003 260)` (light gray surface)
     - `--secondary-foreground: oklch(0.2 0.01 260)`
     - `--muted: oklch(0.96 0.003 260)`
     - `--muted-foreground: oklch(0.5 0.015 260)` (gray text)
     - `--accent: oklch(0.96 0.003 260)`
     - `--accent-foreground: oklch(0.2 0.01 260)`
     - `--destructive: oklch(0.577 0.245 27.325)` (keep existing red)
     - `--border: oklch(0.92 0.005 260)` (subtle border)
     - `--input: oklch(0.92 0.005 260)`
     - `--ring: oklch(0.55 0.2 260)` (blue focus ring)
     - Sidebar tokens with slightly different shade:
       - `--sidebar: oklch(0.975 0.002 260)`
       - `--sidebar-foreground: oklch(0.15 0.01 260)`
       - `--sidebar-primary: oklch(0.55 0.2 260)`
       - `--sidebar-primary-foreground: oklch(0.985 0 0)`
       - `--sidebar-accent: oklch(0.94 0.005 260)`
       - `--sidebar-accent-foreground: oklch(0.15 0.01 260)`
       - `--sidebar-border: oklch(0.92 0.005 260)`
       - `--sidebar-ring: oklch(0.55 0.2 260)`

  3. **.dark**: Replace all color values:
     - `--background: oklch(0.13 0.005 260)` (~#0F1011 near-black)
     - `--foreground: oklch(0.92 0.005 260)` (light gray, not pure white)
     - `--card: oklch(0.17 0.007 260)` (slightly elevated)
     - `--card-foreground: oklch(0.92 0.005 260)`
     - `--popover: oklch(0.17 0.007 260)`
     - `--popover-foreground: oklch(0.92 0.005 260)`
     - `--primary: oklch(0.6 0.18 260)` (blue, slightly brighter)
     - `--primary-foreground: oklch(0.985 0 0)`
     - `--secondary: oklch(0.2 0.007 260)`
     - `--secondary-foreground: oklch(0.92 0.005 260)`
     - `--muted: oklch(0.2 0.007 260)`
     - `--muted-foreground: oklch(0.6 0.015 260)` (~#95A2B3 muted gray)
     - `--accent: oklch(0.2 0.007 260)`
     - `--accent-foreground: oklch(0.92 0.005 260)`
     - `--destructive: oklch(0.704 0.191 22.216)` (keep existing)
     - `--border: oklch(1 0 0 / 10%)` (10% white, Linear pattern)
     - `--input: oklch(1 0 0 / 12%)`
     - `--ring: oklch(0.6 0.18 260)` (blue focus ring)
     - Sidebar tokens:
       - `--sidebar: oklch(0.11 0.005 260)` (darker than background)
       - `--sidebar-foreground: oklch(0.92 0.005 260)`
       - `--sidebar-primary: oklch(0.6 0.18 260)`
       - `--sidebar-primary-foreground: oklch(0.985 0 0)`
       - `--sidebar-accent: oklch(0.22 0.008 260)`
       - `--sidebar-accent-foreground: oklch(0.92 0.005 260)`
       - `--sidebar-border: oklch(1 0 0 / 10%)`
       - `--sidebar-ring: oklch(0.6 0.18 260)`

  4. **Remove the entire glassmorphism utilities block** (the `@layer components` section with `.glass`, `.glass-subtle`, `.glass-input`).

  5. **Keep chart tokens as-is** (no design change needed for charts).

  6. **Keep scrollbar styles** but update colors to use the slight blue tint: change `oklch(0.5 0 0 / 0.3)` to `oklch(0.5 0.005 260 / 0.3)` and `oklch(0.7 0 0 / 0.3)` to `oklch(0.7 0.005 260 / 0.3)` (and similarly for hover states).

  7. **Keep the base layer** (`* { @apply border-border outline-ring/50; }` and `body { @apply bg-background text-foreground; }`).
  </action>
  <verify>
  Run `cd sidekiq-webapp && npx next build --no-lint 2>&1 | tail -20` to confirm no CSS parsing errors. Alternatively, run `pnpm dev` briefly and check console for no CSS errors.
  Grep for "glass" in globals.css: `grep -c "glass" src/styles/globals.css` should return 0.
  Grep for "0.625rem" in globals.css: should return 0 (radius changed).
  Grep for "--font-inter" in globals.css: should return 1+ matches.
  </verify>
  <done>
  globals.css has the complete blue-accent oklch palette for both light/dark modes, --radius is 0.375rem, glassmorphism classes are removed, --font-sans references --font-inter, and --font-mono references --font-geist-mono.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap font from Geist to Inter + Geist Mono</name>
  <files>sidekiq-webapp/src/app/layout.tsx</files>
  <action>
  Replace the Geist font import with Inter + Geist Mono:

  1. Change the import from `import { Geist } from "next/font/google"` to `import { Inter, Geist_Mono } from "next/font/google"`.
     - NOTE: If `Geist_Mono` is not available from `next/font/google`, try `import { JetBrains_Mono } from "next/font/google"` instead and use variable `--font-geist-mono` (or `--font-jetbrains-mono` and update globals.css accordingly).

  2. Replace the font instantiation:
     ```tsx
     const inter = Inter({
       subsets: ["latin"],
       variable: "--font-inter",
       display: "swap",
     });

     const geistMono = Geist_Mono({
       subsets: ["latin"],
       variable: "--font-geist-mono",
     });
     ```

  3. Update the `<html>` className to include both font variables:
     ```tsx
     <html lang="en" className={`${inter.variable} ${geistMono.variable}`} suppressHydrationWarning>
     ```

  4. Remove the old `geist` constant entirely.

  Keep everything else in layout.tsx unchanged (metadata, ThemeProvider, TRPCReactProvider, Toaster).
  </action>
  <verify>
  Run `cd sidekiq-webapp && pnpm tsc --noEmit 2>&1 | head -20` to confirm no TypeScript errors.
  Check that the font variables are correctly referenced: `grep "font-inter" src/app/layout.tsx` should return a match.
  Check that Geist is no longer imported: `grep "Geist" src/app/layout.tsx` should only show Geist_Mono, not standalone Geist.
  </verify>
  <done>
  layout.tsx loads Inter as the primary sans-serif font and Geist Mono as the monospace font. Both are exposed as CSS variables consumed by the @theme block in globals.css.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "glass" sidekiq-webapp/src/styles/globals.css` returns 0
2. `grep "0.375rem" sidekiq-webapp/src/styles/globals.css` finds the radius
3. `grep "oklch(0.55 0.2 260)" sidekiq-webapp/src/styles/globals.css` finds the primary color
4. `grep "Inter" sidekiq-webapp/src/app/layout.tsx` finds the font import
5. `cd sidekiq-webapp && pnpm tsc --noEmit` passes without errors
</verification>

<success_criteria>
- globals.css defines blue-accent oklch palette for both :root and .dark
- --radius is 0.375rem (6px base)
- Glassmorphism classes (.glass, .glass-subtle, .glass-input) are removed
- Inter is loaded as primary sans-serif font
- Geist Mono is loaded as monospace font
- @theme --font-sans references --font-inter
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rethink-branding-ui/08.1-01-SUMMARY.md`
</output>
