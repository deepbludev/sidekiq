---
phase: 08.1-rethink-branding-ui
plan: 06
type: execute
wave: 3
depends_on: ["08.1-01", "08.1-02"]
files_modified:
  - sidekiq-webapp/src/components/sidekiq/sidekiq-card.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-empty-state.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-list.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-form.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-avatar.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-preview.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-indicator.tsx
  - sidekiq-webapp/src/components/sidekiq/sidekiq-picker.tsx
  - sidekiq-webapp/src/components/sidekiq/color-picker.tsx
  - sidekiq-webapp/src/components/sidekiq/emoji-picker-popover.tsx
  - sidekiq-webapp/src/components/sidekiq/instructions-editor.tsx
  - sidekiq-webapp/src/components/sidekiq/conversation-starters.tsx
  - sidekiq-webapp/src/components/sidekiq/starter-templates.tsx
  - sidekiq-webapp/src/components/sidekiq/avatar-picker.tsx
  - sidekiq-webapp/src/components/sidekiq/delete-sidekiq-dialog.tsx
  - sidekiq-webapp/src/components/model-picker/model-picker.tsx
  - sidekiq-webapp/src/components/model-picker/model-picker-content.tsx
  - sidekiq-webapp/src/components/model-picker/model-item.tsx
  - sidekiq-webapp/src/components/model-picker/model-hover-card.tsx
  - sidekiq-webapp/src/components/model-picker/model-picker-trigger.tsx
autonomous: true

must_haves:
  truths:
    - "Sidekiq cards use border border-border bg-card with shadow-sm in light mode"
    - "Sidekiq empty state has clean centered layout with muted icon, no glass"
    - "Sidekiq picker is styled as crisp command palette"
    - "Model picker uses semantic tokens throughout"
    - "No hardcoded zinc colors in any sidekiq or model-picker component"
    - "Avatar shapes maintained: round for sidekiq, rounded-lg for team"
  artifacts:
    - path: "sidekiq-webapp/src/components/sidekiq/sidekiq-card.tsx"
      provides: "Card with border and semantic tokens"
      contains: "border-border"
    - path: "sidekiq-webapp/src/components/sidekiq/sidekiq-picker.tsx"
      provides: "Crisp command palette"
    - path: "sidekiq-webapp/src/components/model-picker/model-picker.tsx"
      provides: "Model picker with semantic tokens"
  key_links:
    - from: "sidekiq-webapp/src/components/sidekiq/sidekiq-card.tsx"
      to: "sidekiq-webapp/src/styles/globals.css"
      via: "Semantic tokens (bg-card, border-border)"
      pattern: "bg-card"
---

<objective>
Restyle all Sidekiq and model picker components to match the Linear-inspired aesthetic: clean cards with borders, crisp command palettes, and consistent semantic token usage.

Purpose: Sidekiq components represent the product's differentiating feature. They need to feel polished with the new design language. Model picker is a frequently accessed component that must be visually consistent.
Output: 20 updated Sidekiq and model picker component files.
</objective>

<execution_context>
@/Users/carlocasorzo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/carlocasorzo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/08.1-rethink-branding-ui/08.1-RESEARCH.md
@.planning/phases/08.1-rethink-branding-ui/08.1-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Sidekiq components</name>
  <files>
    sidekiq-webapp/src/components/sidekiq/sidekiq-card.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-empty-state.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-list.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-form.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-avatar.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-preview.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-indicator.tsx
    sidekiq-webapp/src/components/sidekiq/sidekiq-picker.tsx
    sidekiq-webapp/src/components/sidekiq/color-picker.tsx
    sidekiq-webapp/src/components/sidekiq/emoji-picker-popover.tsx
    sidekiq-webapp/src/components/sidekiq/instructions-editor.tsx
    sidekiq-webapp/src/components/sidekiq/conversation-starters.tsx
    sidekiq-webapp/src/components/sidekiq/starter-templates.tsx
    sidekiq-webapp/src/components/sidekiq/avatar-picker.tsx
    sidekiq-webapp/src/components/sidekiq/delete-sidekiq-dialog.tsx
  </files>
  <action>
  Read each file and replace hardcoded colors with semantic tokens:

  **sidekiq-card.tsx:**
  - Card: `border border-border bg-card` with `shadow-sm dark:shadow-none`
  - Hover: `hover:border-primary/30` (subtle blue hover on card border) or `hover:bg-accent/50`
  - Title: `text-foreground font-medium`
  - Description: `text-sm text-muted-foreground`
  - Replace any hardcoded zinc colors or glass classes

  **sidekiq-empty-state.tsx:**
  - Remove `glass-subtle` class
  - Clean centered layout: icon in `bg-muted rounded-full p-4`, heading in `text-foreground font-semibold`, description in `text-muted-foreground`, primary CTA button
  - Replace any hardcoded zinc colors

  **sidekiq-list.tsx:**
  - Clean list/grid container. Replace hardcoded zinc colors.

  **sidekiq-form.tsx:**
  - Form containers: `bg-card border border-border rounded-lg`
  - Form labels: `text-sm font-medium text-foreground`
  - Form descriptions: `text-xs text-muted-foreground`
  - Replace hardcoded zinc colors

  **sidekiq-avatar.tsx:**
  - Keep `rounded-full` shape for sidekiq avatars (maintain differentiation from team avatars)
  - Replace hardcoded zinc colors if any

  **sidekiq-preview.tsx:**
  - Preview card: `bg-card border border-border rounded-lg`
  - Replace hardcoded zinc colors

  **sidekiq-indicator.tsx:**
  - Badge/indicator: use `bg-primary/10 text-primary` or `bg-muted text-muted-foreground`
  - Replace hardcoded zinc colors

  **sidekiq-picker.tsx:**
  - Style as crisp command palette: `border border-border bg-popover shadow-lg`
  - Items: `hover:bg-accent`
  - Replace hardcoded zinc colors

  **color-picker.tsx, emoji-picker-popover.tsx, avatar-picker.tsx:**
  - Replace hardcoded zinc colors with semantic tokens
  - Popover containers: `border border-border bg-popover`

  **instructions-editor.tsx:**
  - Editor area: `border border-border bg-card rounded-lg`
  - Replace hardcoded zinc colors

  **conversation-starters.tsx, starter-templates.tsx:**
  - Replace hardcoded zinc colors
  - Starter items: `border border-border bg-card hover:bg-accent/50`

  **delete-sidekiq-dialog.tsx:**
  - Use dialog semantic tokens (already handled by UI primitive update)
  - Replace any hardcoded zinc colors in the dialog content
  - Destructive button: `bg-destructive text-destructive-foreground`
  </action>
  <verify>
  Run: `grep -r "zinc-\|glass\|backdrop-blur" sidekiq-webapp/src/components/sidekiq/` — should return no matches.
  Run: `cd sidekiq-webapp && pnpm tsc --noEmit 2>&1 | head -20` — no TypeScript errors.
  </verify>
  <done>
  All 15 Sidekiq component files use semantic tokens. Cards have border+bg-card styling. Empty state is clean. Picker is a crisp command palette. Forms use card containers. Zero hardcoded colors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle model picker components</name>
  <files>
    sidekiq-webapp/src/components/model-picker/model-picker.tsx
    sidekiq-webapp/src/components/model-picker/model-picker-content.tsx
    sidekiq-webapp/src/components/model-picker/model-item.tsx
    sidekiq-webapp/src/components/model-picker/model-hover-card.tsx
    sidekiq-webapp/src/components/model-picker/model-picker-trigger.tsx
  </files>
  <action>
  Read each file and replace hardcoded colors:

  **model-picker.tsx:**
  - Ensure the picker container uses semantic tokens
  - Replace hardcoded zinc colors

  **model-picker-content.tsx:**
  - Command palette content: crisp styling with `border border-border bg-popover`
  - Provider group headers: `text-xs font-medium text-muted-foreground`
  - Replace hardcoded zinc colors

  **model-item.tsx:**
  - Item hover: `hover:bg-accent`
  - Selected/active: `bg-accent text-accent-foreground`
  - Model name: `text-sm text-foreground`
  - Provider badge: `text-xs text-muted-foreground`
  - Favorite star: `text-primary` when active, `text-muted-foreground` when inactive
  - Replace hardcoded zinc colors

  **model-hover-card.tsx:**
  - Hover card: `border border-border bg-popover shadow-md`
  - Feature tags: `bg-muted text-muted-foreground text-xs rounded px-1.5 py-0.5`
  - Replace hardcoded zinc colors

  **model-picker-trigger.tsx:**
  - Trigger button: ghost style or outlined `border border-border`
  - Model name: `text-sm text-foreground`
  - Replace hardcoded zinc colors
  </action>
  <verify>
  Run: `grep -r "zinc-\|glass\|backdrop-blur" sidekiq-webapp/src/components/model-picker/` — should return no matches.
  Run: `cd sidekiq-webapp && pnpm tsc --noEmit 2>&1 | head -20` — no TypeScript errors.
  </verify>
  <done>
  All 5 model picker component files use semantic tokens. Picker content is a crisp command palette. Items have proper hover/active states. Hover card shows model details cleanly. Zero hardcoded colors.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "zinc-\|glass\|backdrop-blur" sidekiq-webapp/src/components/sidekiq/ sidekiq-webapp/src/components/model-picker/` returns no matches
2. `cd sidekiq-webapp && pnpm tsc --noEmit` passes
3. `grep "rounded-full" sidekiq-webapp/src/components/sidekiq/sidekiq-avatar.tsx` confirms avatar shape preserved
</verification>

<success_criteria>
- All 20 Sidekiq and model picker files use semantic tokens only
- Sidekiq cards have border + bg-card with shadow-sm in light mode
- Empty states are clean centered layouts with no glass
- Sidekiq picker is a crisp command palette
- Model picker uses consistent semantic tokens
- Avatar shapes preserved (rounded-full for sidekiq)
- TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/08.1-rethink-branding-ui/08.1-06-SUMMARY.md`
</output>
