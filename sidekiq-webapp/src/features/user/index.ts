/**
 * User feature slice barrel export.
 *
 * Public API for the user feature. External modules should import
 * from `@sidekiq/user` or `@sidekiq/user/*` paths.
 *
 * @module user
 */

// Hooks
export { useViewPreference } from "./hooks/use-view-preference";
